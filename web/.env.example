# ローカルの実値は .env.local に置く（git 管理外）。
# supabase start 後の URL / anon キーを入れる。
# SERVICE_ROLE はサーバー専用。クライアントに出さない。退会の Auth 削除で使う。

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
